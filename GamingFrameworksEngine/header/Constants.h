#pragma once

namespace engine_constant
{
	const float PHYSICS_DELTA_TIME = 1 / 120.0f;
	const float DELTA_TIME_CAP = 0.25f;
    const float MIN_FRAME_TIME = 1 / 60.0f;
    const float GRAVITY = 0.02f;
}